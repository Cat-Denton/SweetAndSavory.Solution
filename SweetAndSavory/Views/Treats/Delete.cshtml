@{
  Layout = "_Layout";
}

@model SweetAndSavory.Models.Treat

<h2>Are you sure you want to delete this treat?</h2>
<hr />
@Html.DisplayNameFor(model => model.Name): @Html.DisplayFor(model => model.Name)
@using (Html.BeginForm())
{
  <input type="submit" value="Delete" />
}
<hr />
<p>@Html.ActionLink("Cancel delete and return to treat", "Details", new {id = Model.TreatId})</p>
<p>@Html.ActionLink("Cancel delete and return to index", "Index")</p>